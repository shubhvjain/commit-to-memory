import { SafeThingPipe } from './safe-thing.pipe';

describe('SafeThingPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeThingPipe();
    expect(pipe).toBeTruthy();
  });
});
